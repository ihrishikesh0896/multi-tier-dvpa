# DevBlog — intentionally vulnerable dependency set
# FOR SECURITY TOOLING TESTS ONLY — do not use in production

# Web framework (CVE-2021-44420, CVE-2022-22818)
Django==3.2.0
djangorestframework==3.12.0
setuptools

# Image processing (CVE-2021-34552, CVE-2023-44271)
Pillow==8.3.1

# YAML parsing — unsafe yaml.load() without Loader (CVE-2020-14343)
PyYAML==5.4.1

# HTTP client — header injection via proxy (CVE-2023-32681)
requests==2.27.1
urllib3==1.26.8
certifi==2021.10.8

# JWT — algorithm confusion attack (CVE-2022-29217 affects PyJWT < 2.4.0)
PyJWT==2.3.0

# HTML sanitisation — mXSS bypass (CVE-2021-23980, fixed in bleach 3.3.1)
bleach==3.3.0
six==1.16.0
webencodings==0.5.1

# XML/RSS parsing — XXE (CVE-2022-2309 in lxml < 4.9.1)
lxml==4.6.5

# Markdown rendering — XSS on raw user input
Markdown==3.3.4

# Cryptography — CVE-2023-49083 affects < 41.0.6
cryptography==36.0.2
cffi==1.15.0

# Database adapter
psycopg2-binary==2.9.3

# WSGI server
gunicorn==20.1.0

# Testing / coverage (instrumented by VulnReach)
pytest==7.0.0
pytest-django==4.5.2
